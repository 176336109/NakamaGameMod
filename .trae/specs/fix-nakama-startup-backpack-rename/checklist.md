# Checklist
- [x] `iap.lua` requires `backpack` instead of `inventory`.
- [x] `iap.lua` uses `backpack.add_items`.
- [x] `gacha.lua` requires `backpack` instead of `inventory`.
- [x] `gacha.lua` uses `backpack.add_items` and `backpack.consume_items`.
- [x] `checkin.lua` requires `backpack` instead of `inventory`.
- [x] `checkin.lua` uses `backpack.add_items` and `backpack.consume_items`.
- [x] `backpack.lua` has no references to `collection = "inventory"`.
- [x] `backpack.lua` has no references to `nk.storage_list(..., "inventory", ...)`.
