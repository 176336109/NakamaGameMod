- [x] 服务端按 `stackable` 与 `hasExpireAt` 独立属性完成存储与判定
- [x] 可堆叠物品追加数量不新增占格，新增记录时正确校验容量
- [x] 不可堆叠时效权益按 VIP 既有实例口径入包与续费
- [x] `expireAt > 当前时间` 作为有效性唯一口径并在查询中生效
- [x] 批量发奖按原子事务执行，任一失败整批回滚
- [x] 同一 `requestId` 发奖与扣减具备幂等行为
- [x] 并发扣减不会导致负库存或重复扣减成功
- [x] 过期清理后 `usedSlotCount` 与有效库存一致
- [x] 水晶按原钻石路径处理，金币维持原处理方式
- [x] 背包 RPC 在服务端注册并可被 SDK 正常调用
- [x] Unity SDK 请求/响应结构与服务端返回字段对齐
- [x] 自动化测试覆盖核心与边界场景并通过
- [x] 《背包系统设计》用例与 `BackpackServiceTests.cs` 覆盖矩阵已复核并记录缺口
